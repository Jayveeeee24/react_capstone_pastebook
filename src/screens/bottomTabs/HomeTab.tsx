import { useEffect } from "react";
import { Text, View } from "react-native";

export const HomeTab = ({navigation}: any) => {
    return (
        <View>
            <Text>HomeTab</Text>
        </View>
    );
}